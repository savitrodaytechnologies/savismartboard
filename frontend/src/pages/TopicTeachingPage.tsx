// Owner: Parivesh (Smartboard core) — composes KBot list (Mukesh) + canvas (Parivesh)
export default function TopicTeachingPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Topic Teaching</h1>
      <p className="text-slate-600 mt-2">Owner: Parivesh — host page for cards + questions + canvas.</p>
    </div>
  );
}
