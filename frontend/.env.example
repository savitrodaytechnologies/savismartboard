# Backend → http://localhost:5000 by default. Frontend dev proxy points at the
# https endpoint Kestrel exposes (https://localhost:7001). Override here if
# your local ports differ.
VITE_API_PROXY_TARGET=https://localhost:7001
